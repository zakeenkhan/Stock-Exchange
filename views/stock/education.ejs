<%- include('../partials/header', { title: 'Educational Resources', activeNav: 'education' }) %>

<div class="container-fluid py-5">
    <div class="container py-5">
        <div class="text-center mx-auto mb-5 wow fadeInUp" data-wow-delay="0.1s" style="max-width: 700px;">
            <h1 class="display-5 mb-4">Educational Resources</h1>
            <p class="mb-4">Access comprehensive learning materials, tutorials, and market insights to improve your investment knowledge and skills.</p>
        </div>

        <!-- Selected Stock Context (if symbol provided) -->
        <% if (requestedSymbol) { %>
        <div class="row mb-4">
            <div class="col-12">
                <div class="alert <%= selectedStock ? 'alert-info' : 'alert-warning' %> d-flex justify-content-between align-items-center">
                    <div>
                        <% if (selectedStock) { %>
                            <strong><%= selectedStock.symbol %></strong> — <%= selectedStock.company_name || selectedStock.name || '' %>
                        <% } else { %>
                            No stock found for symbol "<%= requestedSymbol %>". Showing general education content.
                        <% } %>
                    </div>
                    <div class="ms-3">
                        <% if (selectedStock) { %>
                            <a class="btn btn-sm btn-outline-primary" href="/stock/<%= selectedStock.symbol %>">View Details</a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
        <% } %>

        <!-- Featured Video Tutorials -->
        <div class="row mb-5">
            <div class="col-12">
                <div class="card border-0 shadow-sm">
                    <div class="card-header bg-primary text-white">
                        <h4 class="mb-0">Featured Video Tutorials</h4>
                    </div>
                    <div class="card-body p-4">
                        <div class="row g-4">
                            <% if (typeof videos !== 'undefined' && videos.length > 0) { %>
                                <% videos.forEach(video => { %>
                                    <div class="col-md-6 col-lg-4">
                                        <div class="card h-100">
                                            <div class="position-relative">
                                                <img src="<%= video.thumbnail_url || 'https://via.placeholder.com/600x400?text=Video' %>" class="card-img-top" alt="<%= video.title || 'Video' %>" style="height: 200px; object-fit: cover;">
                                                <div class="position-absolute top-0 end-0 p-2">
                                                    <span class="badge bg-primary"><i class="far fa-clock me-1"></i><%= video.duration || '' %></span>
                                                </div>
                                                <a href="<%= video.video_url || '#' %>" class="btn btn-primary position-absolute top-50 start-50 translate-middle" target="_blank">
                                                    <i class="fas fa-play me-1"></i> Watch Now
                                                </a>
                                            </div>
                                            <div class="card-body">
                                                <h5 class="card-title mb-3"><%= video.title || 'Video' %></h5>
                                                <p class="card-text text-muted"><%= video.description || '' %></p>
                                                <div class="mt-3">
                                                    <span class="badge bg-light text-dark"><%= video.category || 'General' %></span>
                                                </div>
                                                <p class="card-text text-muted mb-2">
                                                    <small>
                                                        <i class="far fa-calendar-alt me-1"></i> 
                                                        <%= video.published_at ? new Date(video.published_at).toLocaleDateString() : 'N/A' %> | 
                                                        <i class="far fa-clock me-1"></i> 
                                                        <%= video.duration || '' %> min
                                                    </small>
                                                </p>
                                                <p class="card-text"><%= (video.description || '').substring(0, 100) %>...</p>
                                                <div class="d-flex justify-content-between align-items-center">
                                                    <span class="badge bg-info"><%= video.category || 'General' %></span>
                                                    <a href="/stock/education/video/<%= video.id %>" class="btn btn-sm btn-outline-primary">Watch Now</a>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <div class="col-12 text-center py-4">
                                    <p>No video tutorials available at the moment.</p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Educational Articles -->
        <div class="row mb-5">
            <div class="col-12">
                <div class="card border-0 shadow-sm">
                    <div class="card-header bg-primary text-white">
                        <h4 class="mb-0">Educational Articles</h4>
                    </div>
                    <div class="card-body p-4">
                        <% if (articles && articles.length > 0) { %>
                            <% articles.forEach(article => { %>
                                <div class="mb-4 pb-4 border-bottom">
                                    <h5><%= article.title || (article.symbol ? ('Article for ' + article.symbol) : 'Article') %></h5>
                                    <p class="text-muted mb-2">
                                        <small>
                                            <i class="far fa-calendar-alt me-1"></i> 
                                            <%= article.published_at ? new Date(article.published_at).toLocaleDateString() : 'N/A' %> | 
                                            <i class="far fa-user me-1"></i> 
                                            <%= article.author || 'Editorial' %> |
                                            <i class="far fa-folder me-1"></i>
                                            <%= article.category || 'General' %>
                                        </small>
                                    </p>
                                    <p><%= article.summary || '' %></p>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div>
                                            <% if (article.tags) { %>
                                                <% article.tags.split(',').forEach(tag => { %>
                                                    <span class="badge bg-secondary me-1"><%= tag.trim() %></span>
                                                <% }); %>
                                            <% } %>
                                        </div>
                                        <a href="/stock/education/article/<%= article.id %>" class="btn btn-sm btn-outline-primary">Read More</a>
                                    </div>
                                </div>
                            <% }); %>
                        <% } else { %>
                            <div class="text-center py-4">
                                <p>No educational articles available at the moment.</p>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>

        <!-- Learning Paths -->
        <div class="row">
            <div class="col-12">
                <div class="card border-0 shadow-sm">
                    <div class="card-header bg-primary text-white">
                        <h4 class="mb-0">Learning Paths</h4>
                    </div>
                    <div class="card-body p-4">
                        <div class="row g-4">
                            <div class="col-md-4">
                                <div class="card h-100">
                                    <div class="card-body text-center">
                                        <i class="fas fa-graduation-cap fa-3x text-primary mb-3"></i>
                                        <h5>Beginner's Guide</h5>
                                        <p>Start your investment journey with fundamental concepts, terminology, and basic strategies.</p>
                                        <a href="/stock/education/path/beginner" class="btn btn-outline-primary">Start Learning</a>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="card h-100">
                                    <div class="card-body text-center">
                                        <i class="fas fa-chart-bar fa-3x text-primary mb-3"></i>
                                        <h5>Technical Analysis</h5>
                                        <p>Master chart patterns, indicators, and technical tools to identify market trends and opportunities.</p>
                                        <a href="/stock/education/path/technical" class="btn btn-outline-primary">Start Learning</a>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="card h-100">
                                    <div class="card-body text-center">
                                        <i class="fas fa-balance-scale fa-3x text-primary mb-3"></i>
                                        <h5>Fundamental Analysis</h5>
                                        <p>Learn to evaluate companies based on financial statements, industry position, and economic factors.</p>
                                        <a href="/stock/education/path/fundamental" class="btn btn-outline-primary">Start Learning</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/footer') %> 